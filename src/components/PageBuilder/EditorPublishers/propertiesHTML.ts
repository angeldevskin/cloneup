import { Property } from '../../../models/editor.model'
import { Editor } from 'grapesjs'

export const propertiesHTML: Property[] = []

export function EditorPropertiesHTML(editor: Editor) {
	console.log(editor)
}
