import styled from 'styled-components'

export const Wrapper = styled.div`
	width: 100%;
	display: flex;

	overflow: auto;

	> div {
		padding-left: 5rem;

		display: flex;
		flex-direction: column;

		width: 100%;
	}
`
